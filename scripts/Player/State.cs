using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Player {
    public enum State {
        NONE = 0,
        NORMAL,
        STUN,
        ILL,
        IMMORTAL,
        WIN_LOSE
    }
}