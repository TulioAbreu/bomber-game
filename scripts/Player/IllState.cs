using UnityEngine;
using UnityEditor;
using System;

namespace Player {
    public enum IllState {
        NONE = 0,
        FAST_PACE,
        LOW_PACE,
        DIARRHEA,
        LOW_POWER,
        CONSTIPATION,
        CHANGE
    }
}